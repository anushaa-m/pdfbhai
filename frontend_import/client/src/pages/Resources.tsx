/** Reference-aligned resources: search and resource actions work entirely against local mock state. */
import { ArrowRight, BookOpen, FileText, LibraryBig, Search, Sparkles } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";
import { Link } from "wouter";
import { AppShell } from "@/components/AppShell";
import { CourseCard, SectionKicker } from "@/components/LearningComponents";
import { courses, resources } from "@/data/mockData";
export default function Resources() { const [query, setQuery] = useState(""); const [opened, setOpened] = useState<string | null>(null); const filtered = useMemo(() => resources.filter(item => `${item.title} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [query]); const openResource = (title: string) => { setOpened(title); toast.success(`${title} opened`, { description: "This demo is showing local learning content." }); }; return <AppShell title="Learning Resources" kicker="Your study ecosystem"><div className="flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><p className="max-w-xl text-lg leading-8 text-[#65728A]">Everything you save, revisit, and practice stays close to the subject it belongs to.</p>{opened && <p className="mt-3 text-sm font-semibold text-[#527148]">Viewing {opened}. Select another resource or continue into practice.</p>}</div><label className="relative w-full lg:w-72"><Search className="absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-[#7A869C]" /><input value={query} onChange={event => setQuery(event.target.value)} className="h-11 w-full rounded-xl border border-[#E4E9F4] bg-white pl-10 pr-4 text-sm outline-none focus:border-[#3B63E8]" placeholder="Search learning resources..." aria-label="Search learning resources" /></label></div><section className="mt-9 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{filtered.length ? filtered.map((resource, index) => <article key={resource.title} className={`resource-card tone-${resource.tone}`}><span className="grid h-10 w-10 place-items-center rounded-xl bg-white/75 text-[#3B63E8] shadow-sm">{index === 0 ? <FileText className="h-5 w-5" /> : index === 1 ? <LibraryBig className="h-5 w-5" /> : index === 2 ? <Sparkles className="h-5 w-5" /> : <BookOpen className="h-5 w-5" />}</span><p className="mt-9 text-xs font-bold uppercase tracking-[.12em] text-[#53617A]">{resource.count}</p><h2 className="mt-2 font-display text-2xl tracking-[-.035em]">{resource.title}</h2><p className="mt-3 text-sm leading-6 text-[#65728A]">{resource.detail}</p><button onClick={() => openResource(resource.title)} className="mt-6 inline-flex items-center gap-1.5 text-sm font-bold text-[#3B63E8]">Open <ArrowRight className="h-4 w-4" /></button></article>) : <div className="col-span-full rounded-2xl border border-dashed border-[#D8DED1] bg-white/70 p-10 text-center text-sm text-[#65728A]">No learning resources match “{query}”.</div>}</section><section className="mt-12"><div className="flex items-end justify-between"><div><SectionKicker>Subjects</SectionKicker><h2 className="font-display text-3xl tracking-[-.045em]">Return to a course when you’re ready.</h2></div><Link href="/upload" className="btn-outline hidden sm:inline-flex">Add notes <ArrowRight className="h-4 w-4" /></Link></div><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-4">{courses.map(course => <CourseCard key={course.id} course={course} />)}</div></section><section className="mt-10 rounded-[1.5rem] bg-[#18233B] p-6 text-white sm:flex sm:items-center sm:justify-between"><div className="flex gap-4"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-white/10 text-[#AFC1FF]"><Sparkles className="h-5 w-5" /></span><div><p className="font-display text-2xl tracking-[-.035em]">Make your next practice set.</p><p className="mt-1 text-sm text-white/65">Upload another study document or continue with a saved subject.</p></div></div><Link href="/upload" className="btn-light mt-5 sm:mt-0">Create Quiz <ArrowRight className="h-4 w-4" /></Link></section></AppShell>; }
